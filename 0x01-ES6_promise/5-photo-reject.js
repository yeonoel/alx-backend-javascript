export default fiunction uploadPhoto(filename) {
  const error = new Error(`${fileName} cannot be processed`)
  const promiseRejected =  Promise.reject(error));
  return promiseRjected.catch((error) => error);

}
